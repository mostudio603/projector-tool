{
  "name": "projector-tool",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Static HTML tools (projector selection, etc.) with a Playwright-based screenshot helper for UI verification.",
  "scripts": {
    "setup:browser": "playwright install --with-deps chromium",
    "shot": "node scripts/screenshot.mjs"
  },
  "devDependencies": {
    "playwright": "^1.60.0"
  }
}
